---
# Development IBM i Inventory
# 
# This file defines the IBM i systems in your development environment.
# Update the hostnames, IP addresses, and connection details to match your infrastructure.

all:
  children:
    ibmi_servers:
      hosts:
        # Example IBM i Development Server 1
        ibmi-dev-01:
          ansible_host: p1369-pvm2.p1369.cecc.ihost.com
          ansible_user: cecuser
          ansible_ssh_private_key_file: ~/.ssh/id_rsa_ibmi_dev
          
          # IBM i specific connection settings
          ansible_shell_type: sh
          ansible_shell_executable: /QOpenSys/pkgs/bin/bash
          
          # Python interpreter - Two-Phase Bootstrap Approach
          # Default to system Python for initial setup (allows Python 3.9 installation)
          # The site.yml playbook overrides this in Phase 2 to use Python 3.9
          # after it has been installed in Phase 1
          ansible_python_interpreter: /QOpenSys/pkgs/bin/python3
          
          # Host-specific variables (optional)
          ibmi_system_name: DEVIBM01
          ibmi_environment: development
          

# Additional groups for development testing
# Example: test different configurations on different hosts
#
#    ibmi_test_group_a:
#      hosts:
#        ibmi-dev-01:
#
#    ibmi_test_group_b:
#      hosts:
#        ibmi-dev-02:

# Made with Bob
